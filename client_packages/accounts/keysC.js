global.Keys = {
    VK_LBUTTON: 0x01,
    VK_RBUTTON: 0x02,
    VK_CANCEL: 0x03,
    VK_MBUTTON: 0x04,
    VK_XBUTTON1: 0x05,
    VK_XBUTTON2: 0x06,
    VK_BACK: 0x08,
    VK_TAB: 0x09,
    VK_CLEAR: 0x0C,
    VK_RETURN: 0x0D,
    VK_SHIFT: 0x10,
    VK_CONTROL: 0x11,
    VK_MENU: 0x12,
    VK_ALT: 0x12,
    VK_PAUSE: 0x13,
    VK_CAPITAL: 0x14,
    VK_KANA: 0x15,
    VK_JUNJA: 0x17,
    VK_FINAL: 0x18,
    VK_HANJA: 0x19,
    VK_ESCAPE: 0x1B,
    VK_CONVERT: 0x1C,
    VK_NONCONVERT: 0x1D,
    VK_ACCEPT: 0x1E,
    VK_MODECHANGE: 0x1F,
    VK_SPACE: 0x20,
    VK_PRIOR: 0x21,
    VK_NEXT: 0x22,
    VK_END: 0x23,
    VK_HOME: 0x24,
    VK_LEFT: 0x25,
    VK_UP: 0x26,
    VK_RIGHT: 0x27,
    VK_DOWN: 0x28,
    VK_SELECT: 0x29,
    VK_PRINT: 0x2A,
    VK_EXECUTE: 0x2B,
    VK_SNAPSHOT: 0x2C,
    VK_INSERT: 0x2D,
    VK_DELETE: 0x2E,
    VK_HELP: 0x2F,
    VK_0: 0x30,
    VK_1: 0x31,
    VK_2: 0x32,
    VK_3: 0x33,
    VK_4: 0x34,
    VK_5: 0x35,
    VK_6: 0x36,
    VK_7: 0x37,
    VK_8: 0x38,
    VK_9: 0x39,
    VK_A: 0x41,
    VK_B: 0x42,
    VK_C: 0x43,
    VK_D: 0x44,
    VK_E: 0x45,
    VK_F: 0x46,
    VK_G: 0x47,
    VK_H: 0x48,
    VK_I: 0x49,
    VK_J: 0x4A,
    VK_K: 0x4B,
    VK_L: 0x4C,
    VK_M: 0x4D,
    VK_N: 0x4E,
    VK_O: 0x4F,
    VK_P: 0x50,
    VK_Q: 0x51,
    VK_R: 0x52,
    VK_S: 0x53,
    VK_T: 0x54,
    VK_U: 0x55,
    VK_V: 0x56,
    VK_W: 0x57,
    VK_X: 0x58,
    VK_Y: 0x59,
    VK_Z: 0x5A,
    VK_LWIN: 0x5B,
    VK_RWIN: 0x5C,
    VK_APPS: 0x5D,
    VK_SLEEP: 0x5F,
    VK_NUMPAD0: 0x60,
    VK_NUMPAD1: 0x61,
    VK_NUMPAD2: 0x62,
    VK_NUMPAD3: 0x63,
    VK_NUMPAD4: 0x64,
    VK_NUMPAD5: 0x65,
    VK_NUMPAD6: 0x66,
    VK_NUMPAD7: 0x67,
    VK_NUMPAD8: 0x68,
    VK_NUMPAD9: 0x69,
    VK_MULTIPLY: 0x6A,
    VK_ADD: 0x6B,
    VK_SEPARATOR: 0x6C,
    VK_SUBTRACT: 0x6D,
    VK_DECIMAL: 0x6E,
    VK_DIVIDE: 0x6F,
    VK_F1: 0x70,
    VK_F2: 0x71,
    VK_F3: 0x72,
    VK_F4: 0x73,
    VK_F5: 0x74,
    VK_F6: 0x75,
    VK_F7: 0x76,
    VK_F8: 0x77,
    VK_F9: 0x78,
    VK_F10: 0x79,
    VK_F11: 0x7A,
    VK_F12: 0x7B,
    VK_F13: 0x7C,
    VK_F14: 0x7D,
    VK_F15: 0x7E,
    VK_F16: 0x7F,
    VK_F17: 0x80,
    VK_F18: 0x81,
    VK_F19: 0x82,
    VK_F20: 0x83,
    VK_F21: 0x84,
    VK_F22: 0x85,
    VK_F23: 0x86,
    VK_F24: 0x87,
    VK_NUMLOCK: 0x90,
    VK_SCROLL: 0x91,
    VK_LSHIFT: 0xA0,
    VK_RSHIFT: 0xA1,
    VK_LCONTROL: 0xA2,
    VK_RCONTROL: 0xA3,
    VK_LMENU: 0xA4,
    VK_RMENU: 0xA5,
    VK_BROWSER_BACK: 0xA6,
    VK_BROWSER_FORWARD: 0xA7,
    VK_BROWSER_REFRESH: 0xA8,
    VK_BROWSER_STOP: 0xA9,
    VK_BROWSER_SEARCH: 0xAA,
    VK_BROWSER_FAVORITES: 0xAB,
    VK_BROWSER_HOME: 0xAC,
    VK_VOLUME_MUTE: 0xAD,
    VK_VOLUME_DOWN: 0xAE,
    VK_VOLUME_UP: 0xAF,
    VK_MEDIA_NEXT_TRACK: 0xB0,
    VK_MEDIA_PREV_TRACK: 0xB1,
    VK_MEDIA_STOP: 0xB2,
    VK_MEDIA_PLAY_PAUSE: 0xB3,
    VK_LAUNCH_MAIL: 0xB4,
    VK_LAUNCH_MEDIA_SELECT: 0xB5,
    VK_LAUNCH_APP1: 0xB6,
    VK_LAUNCH_APP2: 0xB7,
    VK_OEM_1: 0xBA,
    VK_OEM_PLUS: 0xBB,
    VK_OEM_COMMA: 0xBC,
    VK_OEM_MINUS: 0xBD,
    VK_OEM_PERIOD: 0xBE,
    VK_OEM_2: 0xBF,
    VK_OEM_3: 0xC0,
    VK_OEM_4: 0xDB,
    VK_OEM_5: 0xDC,
    VK_OEM_6: 0xDD,
    VK_OEM_7: 0xDE,
    VK_OEM_8: 0xDF,
    VK_OEM_102: 0xE2,
    VK_PROCESSKEY: 0xE5,
    VK_PACKET: 0xE7,
    VK_ATTN: 0xF6,
    VK_CRSEL: 0xF7,
    VK_EXSEL: 0xF8,
    VK_EREOF: 0xF9,
    VK_PLAY: 0xFA,
    VK_ZOOM: 0xFB,
    VK_NONAME: 0xFC,
    VK_PA1: 0xFD,
    VK_OEM_CLEAR: 0xFE
}
let myvehicle = mp.players.local.vehicle;
mp.vehicles.forEachInStreamRange(vehicle =>
{
    if (vehicle.handle !== 0 && myvehicle.handle !== 0 && vehicle !== myvehicle)
    {
        vehicle.setNoCollision(myvehicle.handle, false);
        myvehicle.setNoCollision(vehicle.handle, false);
        vehicle.setAlpha(102);
    }
});
const br_notify = mp.browsers.new("package://accounts/cef/notify/index.html");
// mp.events.add("CEF:NOTIFI:ADD", (type, time, message, title) => {
//     br_notify.call("addNotifi", type, time, message, title);
// });
// // mp.events.call("CEF:NOTIFI:ADD", "warning", 3000, "Текст предупреждения!", "Предупреждение!"); // вызов с клиентской стороны
// // mp.events.call("CEF:NOTIFI:ADD", "error", 3000, "Текст ошибки!", "Ошибка!"); // вызов с клиентской стороны
// // mp.events.call("CEF:NOTIFI:ADD", "success", 3000, "Текст об успехе!", "Успешно!"); // вызов с клиентской стороны
mp.events.add("CEF:NOTIFI:ADD", (type, time, message, title) => {
    br_notify.execute(`window.createNotify('${type}', ${time}, '${message}', '${title}')`);
  });
mp.events.add('freezepers', () =>{ 
    mp.players.local.freezePosition(true);
});
mp.events.add('unfreezePlayer', () =>{ 
    mp.players.local.freezePosition(false);
});
mp.events.add('kursor', () =>{ 
    mp.gui.cursor.show(true, true);
});

let browse;
const localPlayer = mp.players.local;

mp.events.add('playerReady', () => {
    browse = mp.browsers.new('package://accounts/cef/killlist/index.html');
    browse.active = true;
})

mp.events.add('client::displayKillinList', (killer, target, weaponHash) => {
  if (!mp.players.exists(localPlayer)) return;

  if (mp.browsers.exists(browse)) {
    browse.execute(`window.addDataToKillList('${killer}', '${target}', '${weaponHash}', '${localPlayer.name}')`);
  }
})

let brow = mp.browsers.new('package://accounts/cef/pricel(scope)/index.html');
brow.active = false;

const localPlayerr = mp.players.local;

mp.events.add('render', () => {
  if (!mp.players.exists(localPlayerr)) return;
  if (localPlayerr.getVariable('isLogin')) return;
  if (!mp.players.local.getVariable('CustomScope')) return;

  if (mp.game.controls.isControlPressed(0, 25)) {
    mp.game.ui.hideHudComponentThisFrame(14);
    if (mp.game.player.isFreeAiming()) {
    //   const weaponHash = mp.game.invoke(`0x0A6DB4965674D243`, mp.players.local.handle);//если игрок целится со снайперки то кастом прицел пропадает
    //   if (mp.game.weapon.getWeapontypeGroup(weaponHash) == 3082541095) return;
      
      if (mp.browsers.exists(brow)) {
        brow.active = true;
        return;
      }
    }
  }

  if (mp.browsers.exists(brow)) brow.active = false;
});
                        // let pricel;
                        // var pricell = false;
                        // mp.keys.bind(0x4C, true, () => { // Код 0x02 соответствует правой кнопке мыши (ПКМ)
                        //     if (mp.players.local.getVariable('isLogin'))
                        //         return;
                        //     if (mp.players.local.isTypingInTextChat)
                        //         return;
                        //     if (pricell) {
                        //         pricel.destroy();
                        //         pricell = false;
                        //     } else {
                        //         pricell = true;
                        //         pricel =  mp.browsers.new('package://accounts/cef/pricel(scope)/index.html');
                        //     }
                        // });

                        // mp.keys.bind(0x4C, false, () => { // Обработка отпускания ПКМ
                        //     if (pricell) {
                        //         pricel.destroy();
                        //         pricell = false;
                        //     }
                        // });

// // let ekran;
// // var ekrann = false;
// mp.events.add('ekran', () =>{ 
//     if (mp.players.local.getVariable('isLogin'))
//     return;
//     // if (ekrann) {
//     //     ekran.destroy();
//     //     ekran = false;
//     // } else {
//     //     ekrann = true;
//         mp.browsers.new('package://accounts/cef/ekran/index.html');
//     // }
// });
// mp.events.add('revikdostat', (weaponHash, ammo, data) => {
//     if (mp.players.local.getVariable('isLogin')) {
//         return;
//     }
//     if (mp.players.local.isTypingInTextChat) {
//         return;
//     }

//     if (swap === false) {
//         mp.game.invoke('0x28D1A16553C51776', mp.players.local, "anim@weapons@flashlight@stealth", "idle_2_aim_left_med", 0);
//         mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat(data) >> 0, 1000, true, true);
//         // mp.game.invoke("0x3EDCB0505123623B", player.handle);
//         swap = true;
//         setTimeout(function () {
//             swap = false;
//         }, 100);
//     } else {
//         return;
//     }
// });
// mp.events.add('DroppedMaybe', () => {
//     if (mp.players.local.getVariable('isLogin')) {
//         return;
//     }
//     if (mp.players.local.isTypingInTextChat) {
//         return;
//     }
//     mp.events.callRemote('DroppedGun');
// });
mp.events.add('playerDeath', () => {
    mp.game.audio.playSoundFrontend(-1, 'ScreenFlash', 'WastedSounds', true)
});
let swap = false;
let weaponAmmo = {};

mp.events.add('slotik', (weaponHash, ammo, data) => {
    if (mp.players.local.isTypingInTextChat) return;
    if (mp.players.local.getVariable('playerEventsSnipers')) return;
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.getVariable('CaptureIn')) return;

    const currentWeapon = mp.game.invoke("0x0A6DB4965674D243", mp.players.local.handle);
    const currentWeaponHash = currentWeapon >> 0;
    const currentAmmo = mp.game.weapon.getAmmoInClip(mp.players.local.handle, currentWeaponHash) || 5;
    weaponAmmo[currentWeaponHash] = currentAmmo;

    if (!swap) {
        swap = true;
        mp.players.local.removeAllWeapons();
        mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, weaponHash >> 0, 1000, true, true);
        setTimeout(() => {
            if (swap) swap = false;
        }, 75);
        // mp.events.callRemote('syncWeapon', data);
        setTimeout(() => {
          if (weaponHash in weaponAmmo) {
            ammo = weaponAmmo[weaponHash];
            // mp.gui.chat.push(`Reset ammo: ${ammo} for ${weaponHash}`);
          }
          mp.game.weapon.setAmmoInClip(mp.players.local.handle, weaponHash >> 0, ammo);
          mp.events.callRemote('syncWeapon', data);
        }, 20);
    }
});
mp.events.add("playerDeath", (player, reason, killer) => {
    if (player == mp.players.local) {
        weaponAmmo = {};
    }
  });


mp.events.add('c:GivedGun', () => {
    if (mp.players.local.isTypingInTextChat) return;
        mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat('weapon_unarmed') >> 0, 1000, true, true);
});
mp.keys.bind(0x31, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    if (!mp.players.local.getVariable('CaptureIn')) return;
    mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat('weapon_revolver') >> 0, 1000, true, true);
});
mp.keys.bind(0x32, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    if (!mp.players.local.getVariable('playerEventsSnipers')) return;
    mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat('weapon_heavysniper'), 10, true, true);
});
mp.keys.bind(0x33, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    if (!mp.players.local.getVariable('playerEventsSnipers')) return;
    mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat('weapon_heavysniper'), 10, true, true);
});
    // Получаем хэш текущего оружия игрока
//     currentWeaponHash = mp.players.local.weapon;
//     if (currentWeaponHash === weaponHash) {
// //         mp.gui.chat.push(`Текущее оружие: ${currentWeaponHash}`);
// // mp.gui.chat.push(`Новое оружие: ${weaponHash}`);
//         return;
//     }

                    // let swap = false;
                    // mp.events.add('slotik', (weaponHash, ammo, data) => {
                    //     if (mp.players.local.getVariable('isLogin')) {
                    //         return;
                    //     }
                    //     if (mp.players.local.isTypingInTextChat) {
                    //         return;
                    //     }
                    //     if(!swap) {
                    //         swap = true;
                    //         mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, weaponHash >> 0, 1000, true, true);
                    //         setTimeout( function() {
                    //             if (swap) swap = false;
                    //         }, 2000);
                    //         mp.events.callRemote('syncWeapon', weaponHash, ammo, data);
                    //     }
                    // });
// mp.events.add('render', () => {//анимка после выстрела
//     mp.game.invoke("0xC54C95DA968EC5B5", mp.players.local, false);
// })
mp.gui.chat.show(false); //Disables default RageMP Chat
const chat = mp.browsers.new('package://accounts/cef/chat/index.html');
chat.markAsChat();
// mp.keys.bind(0x33, true, () => {
//     if (mp.players.local.getVariable('isLogin'))
//     return;
//     if (mp.players.local.isTypingInTextChat)
//         return;
//         mp.events.callRemote('checkhevik');
// });
// mp.events.add('hevikdostat', () =>{
//     if (mp.players.local.getVariable('isLogin'))
//     return;
//     if(swap === false) {
//         swap = true;
//         mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat(fastSlot) >> 0, 1000, true, true);
//         setTimeout( function() {
//             swap = false;
//         }, 2000);
//     } else {
//     return;
// }
// });
// mp.keys.bind(0x35, true, () => {
//     if (mp.players.local.isTypingInTextChat)
//         return;
//         mp.events.callRemote('vipppp');
// }); 
let Browser;
mp.keys.bind(0x34, true, () => {
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.isTypingInTextChat) return;
    if (mp.players.local.getVariable('CaptureIn')) return;
    // if (mp.players.local.vehicle) return;
    if (mp.players.local.isFalling()) return;

    if (mp.players.local.getVariable('isSwitchArmour')) return;

    // mp.players.local.freezePosition(true);
    mp.events.callRemote('armm');

    setTimeout(() => {
        // mp.players.local.freezePosition(false);
        mp.events.callRemote('arm', [ 123, 456 ]);
    }, 2000)
});
mp.keys.bind(0x35, true, () => {
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.isTypingInTextChat) return;
    if (mp.players.local.isFalling()) return;
    if (mp.players.local.getVariable('CaptureIn')) return;

    if (mp.players.local.getVariable('isHealed')) {
        mp.gui.chat.push('Слишком быстро!');
        return;
    }
    
    mp.events.callRemote('heal');
});
// mp.keys.bind(0x5A, true, () => {
//     if (mp.players.local.getVariable('isLogin'))
//     return;
//     if (mp.players.local.isTypingInTextChat)
//         return;
//     //mp.game.invoke('0xBF0FD6E56C964FCB', mp.players.local.handle, mp.game.joaat('weapon_revolver'), 10, true, true);
//     mp.events.callRemote('hpp', [ 123, 456 ]);
//     mp.gui.chat.push("[Отладка]: Ты умер"); 
// });
// var anim = false;
// mp.keys.bind(0x58, true, () => {
//     if(player.setVariable("isLogin", true)){
//         return;
//         }
//         if (mp.players.local.vehicle) {
//             return;
//             }
//     if (anim) {
//         player.stopAnimation();
//         anim = false;
//     } else {
//         anim = true;
//         player.playAnimation("move_crawlprone2crawlfront" , "front", 0);
//     }
// });
let crawlPlayer = mp.players.local;
crawlPlayer.crawling = false;
let crawlInterval;
let currentAnim;

mp.keys.bind(0x58, true, () => { // Z Key
    if (crawlPlayer.vehicle || crawlPlayer.isTypingInTextChat) return;

    if (crawlPlayer.crawling) {
        stopCrawling();
    } else {
        startCrawling();
    }
});

function startCrawling() {
    crawlInterval = setInterval(handleControls, 0);
    crawlPlayer.crawling = true;
    mp.game.streaming.requestAnimDict('move_crawlprone2crawlfront');
    crawlPlayer.taskPlayAnim(
        'move_crawlprone2crawlfront',
        'front',
        8.0,
        1000,
        -1,
        2,
        0,
        false,
        false,
        false
    );
}

function stopCrawling() {
    crawlPlayer.crawling = false;
    clearInterval(crawlInterval);
    crawlPlayer.clearTasks();
    crawlPlayer.clearSecondaryTask();
}

function handleControls() {
    if (!crawlPlayer.crawling) return;

    let rotation = crawlPlayer.getRotation(2);
    mp.game.controls.disableControlAction(0, 32, true);
    mp.game.controls.disableControlAction(0, 33, true);
    mp.game.controls.disableControlAction(0, 34, true);
    mp.game.controls.disableControlAction(0, 35, true);

    if (mp.game.controls.isDisabledControlPressed(0, 34)) {
        crawlPlayer.setRotation(rotation.x, rotation.y, rotation.z + 0.2, 2, true);
    }

    if (mp.game.controls.isDisabledControlPressed(0, 35)) {
        crawlPlayer.setRotation(rotation.x, rotation.y, rotation.z - 0.2, 2, true);
    }

    if (mp.game.controls.isDisabledControlPressed(0, 32)) {
        playCrawlAnimation('onfront_fwd');
    }

    if (mp.game.controls.isDisabledControlPressed(0, 33)) {
        playCrawlAnimation('onfront_bwd');
    }
}

function playCrawlAnimation(animation) {
    if (currentAnim === animation) return;

    let dict = 'move_crawl';
    let duration = mp.game.entity.getEntityAnimDuration(dict, animation);

    mp.game.streaming.requestAnimDict(dict);
    crawlPlayer.taskPlayAnim(dict, animation, 8.0, 1000, -1, 2, 0, false, false, false);

    currentAnim = animation;

    setTimeout(() => {
        currentAnim = undefined;
    }, (duration - 0.1) * 1000);
}
//==================== [ АНТИ ДБ ] =================
mp.events.add('render', () => {
    if (!mp.players.exists(mp.players.local)) return;
  
    mp.vehicles.forEachInStreamRange((vehicle) => {
      mp.players.local.setNoCollision(vehicle.handle, false);
      vehicle.setNoCollision(mp.players.local.handle, false);
    })
  
    mp.players.forEachInStreamRange((player) => {
      mp.players.local.setNoCollision(player.handle, false);
      player.setNoCollision(mp.players.local.handle, false);
    });
  
    if (mp.players.local.vehicle) {
      mp.vehicles.forEachInStreamRange((vehicle) => {
        mp.players.local.vehicle.setNoCollision(vehicle.handle, false);
        vehicle.setNoCollision(mp.players.local.vehicle.handle, false);
      })
  
      mp.players.forEachInStreamRange((player) => {
        mp.players.local.vehicle.setNoCollision(player.handle, false);
        player.setNoCollision(mp.players.local.vehicle.handle, false);
      })
    }
  })
var cursorVisible = false;
let browserr;
mp.keys.bind(0x71, true, () => {
    if (cursorVisible) {
        mp.gui.cursor.show(false, false);
        cursorVisible = false;
    } else {
        mp.gui.cursor.show(true, true);
        cursorVisible = true;
    }
});

let browserrr;
var auto = false;
mp.keys.bind(0x26, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.getVariable('DrygoiSoxik'))
    return;
    if (mp.players.local.getHealth() < 1) return;
    if (mp.players.local.getVariable('CaptureIn')) return;
    if (auto) {
        // const start = Date.now();
        browserrr.destroy();
        auto = false;
        if (mp.players.local.vehicle) {
            return;
        }
        mp.events.callRemote('autoSTOP', [123, 456 ])
        mp.gui.cursor.show(false, false);
        // Date.now() - start;
    } else {
        auto = true;
        mp.gui.cursor.show(true, true);
        // setTimeout(() => {
            mp.events.callRemote('autoSTART');
            browserrr = mp.browsers.new('package://accounts/cef/telephone/index.html');
            mp.events.callRemote('s:checkCars');
            //auto = true;
        // }, 500)
    }
});

mp.events.add('c:executeAuto', (arrayCars) => {
    browserrr.execute(`window.carShow('${(arrayCars)}');`);
});

mp.events.add('client::checkFrogger2', (frogger2, avtr, divo, havok, gemera, g63amg, caddy, bati2, italirsx) => {
    if (!mp.browsers.exists(browserrr)) return;
    browserrr.execute(`window.checkFrogger2(${frogger2}, ${avtr}, ${divo}, ${havok}, ${gemera}, ${g63amg}, ${caddy}, ${bati2}, ${italirsx})`);
});
// mp.events.add('client::checkFrogger2', (vehicle) => {
//     if (!mp.browsers.exists(browserrr)) return;
//     browserrr.execute(window.addVehicles("${vehicle}"));
// });
let cefCapture;
let cefCapturee = false;

mp.events.add('c:createCefCapture', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.getVariable('DrygoiSoxik'))
        return;

    cefCapture = mp.browsers.new('package://accounts/cef/captureInvite/index.html');
    cefCapturee = true;

    setTimeout(() => {
        mp.events.callRemote('s:endCapture');
        if (cefCapture && cefCapturee) {
            cefCapture.destroy();
            cefCapture = null;
            cefCapturee = false;
        }
    }, 11000);
});


mp.keys.bind(0x59, true, () => {
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.getVariable('DrygoiSoxik')) return;
    if (!mp.players.local.getVariable('waitAccept')) return;
    if (cefCapture && cefCapturee) {
        cefCapture.destroy();
        cefCapture = null;
        cefCapturee = false;
    mp.events.callRemote('s:joinCapture');
    }
});



let InfoCaptureCef;
let InfoCaptureCeff = false;

mp.events.add('c:createCefInfoCapture', () => {
    // if (mp.players.local.getVariable('isLogin'))
    // return;
    if (mp.players.local.getVariable('DrygoiSoxik')) return;
    if (!mp.players.local.getVariable('CaptureIn')) return;
    InfoCaptureCef = mp.browsers.new('package://accounts/cef/hudCapture/index.html');
    InfoCaptureCeff = true;
    mp.gui.cursor.show(false, false);

    menu.active = false;
    menuu = false;
    if(auto){
        browserrr.destroy();
    }
    auto = false;

    // setTimeout(() => {
    //     mp.events.callRemote('s:endCapture');
    //     if (InfoCaptureCef && InfoCaptureCeff) {
    //         InfoCaptureCef.destroy();
    //         InfoCaptureCef = null;
    //         InfoCaptureCeff = false;
    //     }
    // }, 15000);
});
mp.events.add('client::InfoCaptureUpdate', (red, blue) => {
    if (!mp.browsers.exists(InfoCaptureCef)) return;
    InfoCaptureCef.execute(`window.InfoCapture(${red}, ${blue})`)
});

mp.events.add('client::InfoStatisticsCapture', (kill, kd) => {
    if (!mp.browsers.exists(InfoCaptureCef)) return;
    InfoCaptureCef.execute(`window.InfoStatisticsCapture(${kill}, ${kd})`)
});
mp.events.add('c:CloseCefInfoCapture', () => {
    // if (mp.players.local.getVariable('isLogin'))
    // return;
    if (mp.players.local.getVariable('DrygoiSoxik')) return;
    // if (!mp.players.local.getVariable('CaptureIn')) return;
    // InfoCaptureCef = mp.browsers.new('package://accounts/cef/hudCapture/index.html');
    // InfoCaptureCeff = true;

        // mp.events.callRemote('s:endCapture');
        if (InfoCaptureCef && InfoCaptureCeff) {
            InfoCaptureCef.destroy();
            InfoCaptureCef = null;
            InfoCaptureCeff = false;
        }
});
let menu;
let menuu = false;
mp.events.add('playerReady', () => {
    menu = mp.browsers.new('package://accounts/cef/mainmenu/index.html');
    menu.active = false;
})
mp.keys.bind(0x72, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.getVariable('DrygoiSoxik'))
    return;
    if (mp.players.local.getVariable('CaptureIn')) return;
    if (menuu) {
        if (menu.active) {
            menu.active = false;
            mp.gui.cursor.show(false, false);
        } else {
            menu.active = true;
            menu.active = true;
            mp.gui.cursor.show(true, true);
        }
    } else {
        menuu = true;
        mp.gui.cursor.show(true, true);
        mp.events.callRemote('pizdanaxyiXD');
    }
    mp.keys.bind(0x1B, false, () => {
        if (menuu) {
            menu.active = false;
            mp.gui.cursor.show(false, false);
            menuu = false;
        }
    });
});

mp.events.add('openmenu', () =>{ 
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.getVariable('DrygoiSoxik'))
    return;
    if (mp.players.local.getVariable('CaptureIn')) return;
    if (menuu) {
        if (menu.active) {
            menu.active = false;
            mp.gui.cursor.show(false, false);
        } else {
            menu.active = true;
            mp.gui.cursor.show(true, true);
        }
    } else {
        menuu = true;
        mp.gui.cursor.show(true, true);
        // menu = mp.browsers.new('package://accounts/cef/dadada/index.html');
        menu.active = true;
        mp.events.callRemote('pizdanaxyiXD');
    }
    // mp.keys.bind(0x1B, false, () => {
    //     if (menuu) {
    //         menu.destroy();
    //         mp.gui.cursor.show(false, false);
    //         menuu = false;
    //     }
    // });
});

mp.events.add('c:FindPlayer', (textPlayer) => {
    mp.events.callRemote('s:FindPlayer', textPlayer)
})

// let createItem;
// let createItemm = false;
// mp.keys.bind(0x75, true, () => {
//     if (mp.players.local.getVariable('isLogin'))
//     return;
//     if (mp.players.local.getVariable('DrygoiSoxik'))
//     return;
//     if (mp.players.local.getVariable('playerEventsSnipers')) return;
//     if (createItemm) {
//         if (createItem.active) {
//             createItem.active = false;
//             mp.gui.cursor.show(false, false);
//         } else {
//             createItem.active = true;
//             mp.gui.cursor.show(true, true);
//         }
//     } else {
//         createItemm = true;
//         mp.gui.cursor.show(true, true);
//         createItem = mp.browsers.new('package://accounts/cef/craft/index.html');
//         mp.events.callRemote('pizdanaxyiXD');
//     }
//     mp.keys.bind(0x1B, false, () => {
//         if (createItemm) {
//             menu.active = false;
//             mp.gui.cursor.show(false, false);
//             createItemm = false;
//         }
//     });
// });


                // let menu;
                // var menuu = false;
                // mp.keys.bind(0x72, true, () => {
                //     if (mp.players.local.getVariable('isLogin'))
                //     return;
                //     // if (mp.players.local.socialClub !== socialClub) {
                //     //     return;
                //     // }
                //     if (mp.players.local.getVariable('DrygoiSoxik'))
                //     return;
                //     if (menuu) {
                //         menu.destroy();
                //         // mp.events.callRemote('autoSTOP', [123, 456 ])
                //         mp.gui.cursor.show(false, false);
                //         menuu = false;
                //     } else {
                //         menuu = true;
                //         mp.gui.cursor.show(true, true);
                //         // menu = mp.browsers.new('package://accounts/cef/dadada/index.html');
                //         menu = mp.browsers.new('package://accounts/cef/mainmenu/index.html');
                //         mp.events.callRemote('pizdanaxyiXD');
                //         menuu = true;
                //     }
                //     mp.keys.bind(0x1B, false, () => {
                //         if (menuu) {
                //             menu.destroy();
                //             mp.gui.cursor.show(false, false);
                //             menuu = false;
                //         }
                //     });
                // });
                // mp.events.add('openmenu', () =>{ 
                //     if (mp.players.local.getVariable('isLogin'))
                //     return;
                //     if (mp.players.local.getVariable('DrygoiSoxik'))
                //     return;
                //     if (menuu) {
                //         menu.destroy();
                //         // mp.events.callRemote('autoSTOP', [123, 456 ])
                //         mp.gui.cursor.show(false, false);
                //         menuu = false;
                //     } else {
                //         menuu = true;
                //         mp.gui.cursor.show(true, true);
                //         // menu = mp.browsers.new('package://accounts/cef/dadada/index.html');
                //         menu = mp.browsers.new('package://accounts/cef/mainmenu/index.html');
                //         mp.events.callRemote('pizdanaxyiXD');
                //         menuu = true;
                //     }
                //     mp.keys.bind(0x1B, false, () => {
                //         if (menuu) {
                //             menu.destroy();
                //             mp.gui.cursor.show(false, false);
                //             menuu = false;
                //         }
                //     });
                // });










mp.events.add('openGoldCase', () => {
    mp.events.callRemote('s:openGoldCase');
})


mp.events.add('client::playerDonate', (donate, FreeCase) => {
    if (!mp.browsers.exists(menu)) return;
    // mp.gui.chat.push(donate + FreeCase);
    menu.execute(`window.playerDonate("${donate}", "${FreeCase}")`)
});

// let animka;
// var animkaa = false;
// mp.keys.bind(0x58, true, () => {
//     if (mp.players.local.getVariable('isLogin')) return;
//     if (mp.players.local.isTypingInTextChat) return;
//     if (mp.players.local.vehicle) return;
//     if (mp.players.local.isFalling()) return;
//     if (animkaa) {
//         mp.events.callRemote('animEND');
//         animkaa = false;
//     } else {
//         mp.events.callRemote('animSTART');
//         animkaa = true;
//     }
// });


// let pizdec;
// mp.events.add('opensniper', () => {
//     pizdec = mp.browsers.new('package://accounts/cef/eventSnipers/index.html');
// });
mp.events.add('CefGreenZoneExit', () => {
    if (!mp.browsers.exists(greenZone)) return;
    if(greenZone){
    if (!mp.browsers.exists(greenZone)) return;
    greenZone.destroy();
    }
});
mp.events.add('c:clearguns', () => {
    mp.players.local.removeAllWeapons();
});
let banned;
mp.events.add('tablBannedPlayersAll', () => {
    banned = mp.browsers.new('package://accounts/cef/banned/index.html');
});

mp.events.add('client::playerBanned', (name, static, reason) => {
    if (!mp.browsers.exists(banned)) return;
    banned.execute(`window.playerBanned("${name}", "${static}", "${reason}")`)
});



// let respawn;
// var respawnn = false;
let kill;
let sbivanimAlreadyCalled = false;

mp.events.add('playerReady', () => {
    kill = mp.browsers.new('package://accounts/cef/kill/index.html');
    kill.active = false;
});
mp.events.add('respawnn', () => {
    if (mp.players.local.getVariable('isLogin')) return;
    mp.gui.cursor.show(true, true);
    if (!kill.active) {
        kill.active = true;
        mp.gui.cursor.show(true, true);
        // mp.players.local.setCanRagdoll(true);
    }

        if (!sbivanimAlreadyCalled) {
                sbivanimAlreadyCalled = true;
                // mp.events.callRemote('sbivanim');
                mp.events.callRemote('hpp');
        }
});

mp.events.add('respgo', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;

    // Check if the browser exists before destroying
    if (kill.active) {
        kill.active = false;
        mp.gui.cursor.show(false, false);
        // mp.players.local.setCanRagdoll(true);
    }

    mp.events.callRemote('respawn');
});
mp.events.add('respawnnn', () =>{ 
    if (mp.players.local.getVariable('isLogin'))
    return;
    // if (respawnn){
    //     mp.events.callRemote('respawnn')
    //     respawnn = false;
    //     respawn.destroy();
    //     mp.gui.cursor.show(false, false);
    // } else {
    //     respawnn = true;
    //     mp.gui.cursor.show(true, true);
        //respawn = mp.browsers.new('package://accounts/cef/respawn/index.html')
        mp.events.callRemote('respawnnnn')
    //}
});
/*let donate;
var donatee = false;
mp.keys.bind(0x58, true, () => {
    if (mp.players.local.isTypingInTextChat)
    return;
    if (donatee){
        mp.events.callRemote('vipp')
        donatee = false;
        donate.destroy();
        mp.gui.cursor.show(false, false);
    } else {
        donatee = true;
        mp.gui.cursor.show(true, true);
        donate = mp.browsers.new('package://accounts/cef/donate/index.html')
    }
});*/

global.browser;
mp.events.add('playerReady', () => {
    browser = mp.browsers.new('package://accounts/cef/logo/index.html');
});
mp.events.add('offcamera', () => {
    mp.game.cam.renderScriptCams(false, false, 0, true, false);
});
mp.events.add('playerReady', () => {
    const sceneryCamera = mp.cameras.new('default', new mp.Vector3(2457.238, 4952.088, 50.152, 47.189212799072266), new mp.Vector3(), 50);
    sceneryCamera.setActive(true);
    mp.game.cam.setGameplayCamRelativeHeading(0);
    mp.game.cam.renderScriptCams(true, false, 0, true, false);
});
mp.events.add('client::changeHudInfo', (static, name, money, FreeCaseTime, AutoCaseTime, kill, death) => {
    if (!mp.browsers.exists(browser)) return;
    browser.execute(`window.changeMainInfo("${static}", "${name}", "${money}", "${FreeCaseTime}", "${AutoCaseTime}", "${kill}", "${death}")`)
});

mp.events.add('client::changeHudPlayers', () => {
  if (!mp.browsers.exists(browser)) return;
  const playerCount = mp.players.toArray().length;

  browser.execute(`window.changePlayerCount(${playerCount})`);
})


mp.events.add('render', () => {
  if (!mp.browsers.exists(browser)) return;
  if (!mp.players.exists(mp.players.local)) return;

  const weapon = mp.players.local.weapon;
  if (!weapon) return;

  const ammoClip = mp.players.local.getAmmoInClip(weapon);
  const allAmount = mp.game.weapon.getWeaponTintCount(weapon);

  browser.execute(`window.changeAmmoCount("${ammoClip}", "${allAmount}")`)
})
// mp.events.add('moneyy', () => {
//     mp.browsers.new('package://accounts/cef/money/index.html');
// })
/*let vipp;
mp.events.add('vip', () => {
    mp.events.callRemote('vipp')
    vipp.destroy();
    vipp = mp.browsers.new('package://accounts/cef/logo/index.html');
    mp.gui.cursor.show(false, false);
});*/
// let voice;
// var voicee = false;
//  mp.keys.bind(0x4E, true, () => {
//     if (mp.players.local.getVariable('isLogin'))
//     return;
//     if (mp.players.local.isTypingInTextChat)
//         return;
//     mp.events.callRemote('voiceee')
//     if (voicee) {
//         voicee = false;
//     } else {
//         voicee = true;
//         // voice = mp.browsers.new('package://accounts/cef/odezda/index.html');
//     }
// });
mp.events.add('frogger2', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('frogger22')
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
});

mp.events.add('avtr', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('avtrr')
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
});
mp.events.add('divo', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('divoo');
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
});
mp.events.add('havok', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('havokk');
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
});
mp.events.add('gemera', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('gemeraa');
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
});
mp.events.add('g63amg', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('g63amgg');
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
});
mp.events.add('audi', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('vehhhh', [123, 456 ])
    //browserrr.destroy();
    mp.gui.cursor.show(false, false);
 });
mp.events.add('ballas', () => {
    mp.events.callRemote('ballass');
    menu.active = false;
    mp.gui.cursor.show(false, false);
});    
mp.events.add('fam', () => {
    mp.events.callRemote('famm');
    menu.active = false;
    mp.gui.cursor.show(false, false);
});
mp.events.add('vagos', () => {
    mp.events.callRemote('vagoss');
    menu.active = false;
    mp.gui.cursor.show(false, false);
});
mp.events.add('mara', () => {
    mp.events.callRemote('maraa');
    menu.active = false;
    mp.gui.cursor.show(false, false);
});
mp.events.add('fbi', () => {
    mp.events.callRemote('fbii');
    menu.active = false;
    mp.gui.cursor.show(false, false);
});
mp.events.add('bloods', () => {
    mp.events.callRemote('bloodss');
    menu.active = false;
    mp.gui.cursor.show(false, false);
});

mp.events.add('buygun', (argumentss) => {
    mp.events.callRemote('buygunn', argumentss);
});
mp.events.add('giveclothes', (argsclothes) => {
    mp.events.callRemote('s:clothes', argsclothes)
});
mp.events.add('givehair', (argshair) => {
    mp.events.callRemote('s:givehair', argshair)
});
mp.events.add('giveUndershirts', () => {
    mp.events.callRemote('s:giveUndershirts')
});
mp.events.add('givedAcs', (argsAcs) => {
    mp.events.callRemote('s:givedAcs', argsAcs)
});
mp.events.add('buyAcs', (argsBuyAcs) => {
    mp.events.callRemote('s:buyAcs', argsBuyAcs)
});
mp.events.add('buysaiga', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buysaigaa');
});
mp.events.add('buycarbinerifle', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buycarbineriflee');
});
mp.events.add('buygusenberg', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buygusenbergg');
});
mp.events.add('buyhevikmk1', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyhevikmk11');
});
mp.events.add('buyhevikmk2', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyhevikmk22');
});
mp.events.add('buyrevikmk2', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyrevikmk22');
});
mp.events.add('buyprecisionrifle', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyprecisionriflee');
});
mp.events.add('buymarks', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buymarkss');
});
mp.events.add('buyarm', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyarmm');
});
mp.events.add('buyheal', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyheall');
});


mp.events.add('heviksell', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('hevikselll');
});
mp.events.add('hevikmk2sell', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('hevikmk2selll');
});
mp.events.add('riflesell', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('rifleselll');
});

mp.events.add('defolt', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('defoltt');
});

mp.events.add('jen', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('jenn');
});

mp.events.add('kyrica', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('kyricac');
});
mp.events.add('kot', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('kott');
});
mp.events.add('obezana', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('obezanaa');
});

mp.events.add('pig', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('pigg');
});
mp.events.add('vorona', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('voronaa');
});

mp.events.add('customScopeOnOff', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('customScopeOnOfff');
});

mp.events.add('buyfrogger2buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyfrogger2buyy');
});
mp.events.add('buyavtrbuy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buyavtrbuyy');
});
mp.events.add('buydivobuy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buydivobuyy');
});
mp.events.add('buygemerabuy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buygemerabuyy');
});
mp.events.add('buybatibuy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buybatibuyy');
});
mp.events.add('OpenCarsCase', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('OpenCarsCasee');
});
mp.events.add('buydonateavtr', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buydonateavtrr');
});
mp.events.add('buydonatedivo', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buydonatedivoo');
});
mp.events.add('buydonatefrogger2', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('buydonatefrogger22');
});
mp.events.add('f1buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('f1buyy');
});
mp.events.add('f2buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('f2buyy');
});
mp.events.add('f3buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('f3buyy');
});
mp.events.add('f4buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('f4buyy');
});
mp.events.add('f5buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('f5buyy');
});
mp.events.add('f6buy', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('f6buyy');
});
mp.events.add('nadetf1', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('nadetf11');
});
mp.events.add('nadetf2', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('nadetf22');
});
mp.events.add('nadetf3', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('nadetf33');
});
mp.events.add('nadetf4', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('nadetf44');
});
mp.events.add('nadetf5', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('nadetf55');
});
mp.events.add('nadetf6', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('nadetf66');
});

            mp.events.add('buyvip1', () => {
                if (mp.players.local.getVariable('isLogin'))
                return;
                mp.events.callRemote('buyvip11');
            });
            mp.events.add('buyvip2', () => {
                if (mp.players.local.getVariable('isLogin'))
                return;
                mp.events.callRemote('buyvip22');
            });
            mp.events.add('buyvip3', () => {
                if (mp.players.local.getVariable('isLogin'))
                return;
                mp.events.callRemote('buyvip33');
            });
// let odezda;
// var odezdaa = false;
//  mp.keys.bind(0x55, true, () => {
//     if (mp.players.local.isTypingInTextChat)
//         return;
//     if (odezdaa) {
//         odezda.destroy();
//         mp.events.callRemote('odezda')
//         mp.gui.cursor.show(false, false);
//         odezdaa = false;
//     } else {
//         odezdaa = true;
//         mp.gui.cursor.show(true, true);
//         //const start = Date.now();
//         //setTimeout(() => {
//             //mp.events.callRemote('autoSTART');
//             odezda = mp.browsers.new('package://accounts/cef/odezda/index.html');
//             //auto = true;
//             //Date.now() - start;
//         //}, 500)
//     }
// });

mp.events.add('odezda', () => {
    mp.events.callRemote('odezdaa')
    //odezda.destroy();
    mp.gui.cursor.show(true, true);
 });

mp.events.add('kepka', () => {
    mp.events.callRemote('kepkaa')
    //odezda.destroy();
    mp.gui.cursor.show(true, true);
 });

 mp.events.add('sumka', () => {
    mp.events.callRemote('sumkaa')
    //odezda.destroy();
    mp.gui.cursor.show(true, true);
 });

 mp.events.add('close', () => {
    mp.events.callRemote('closee')
    //odezda.destroy();
    mp.gui.cursor.show(true, true);
 });


 mp.events.add('aga', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('playerDeath');
    if (!mp.players.exists(mp.players.local)) return;
        const position = mp.players.local.position;
        mp.events.callRemote('createObject', position.x, position.y, position.z);
});
let inZone = false;
let grenZones;
mp.events.add('playerReady', () => {
    grenZones = mp.browsers.new('package://accounts/cef/greenzone/index.html');
    grenZones.active = false;
});
mp.events.add('client::enterGreenZone', () => {
  inZone = true;
  if (!grenZones.active){
    grenZones.active = true;
  }
});

mp.events.add('client::exitGreenZone', () => {
  inZone = false;
  if (grenZones.active){
    grenZones.active = false;
  }
});

mp.events.add('render', ()=> {
  if (inZone) {
    mp.game.controls.disableControlAction(0, 24, true);
    mp.game.controls.disableControlAction(0, 25, true);
    mp.game.controls.disableControlAction(0, 69, true);
    mp.game.controls.disableControlAction(0, 70, true);
    mp.game.controls.disableControlAction(0, 92, true);
    mp.game.controls.disableControlAction(0, 114, true);
    mp.game.controls.disableControlAction(0, 121, true);
    mp.game.controls.disableControlAction(0, 140, true);
    mp.game.controls.disableControlAction(0, 141, true);
    mp.game.controls.disableControlAction(0, 142, true);
    mp.game.controls.disableControlAction(0, 257, true);
    mp.game.controls.disableControlAction(0, 263, true);
    mp.game.controls.disableControlAction(0, 264, true);
    mp.game.controls.disableControlAction(0, 331, true);
  }
})
// mp.keys.bind(0x45, true, () => {
//     const nearbyObjects = mp.game.object.getAllInRange(mp.players.local.position,5, true);
//     // let nearbyObjects = mp.objects.getInRange(player.position, 5);
//     if (nearbyObjects.length >= 1) {
//         mp.gui.chat.push("Рядом");
//     } else {
//         mp.gui.chat.push("Далеко");
// }
// });
// const revik = {
//     // x: 0,
//     // y: 0,
//     // z: 0,
//     model: 3249783761
// }
                // mp.events.add('playerEnterColshapeee', () => {
                //     mp.events.callRemote('playerEnterColshapeeee');
                // })
                // mp.keys.bind(0x45, true, () => {
                //     // mp.events.callRemote('closee');
                //     mp.events.callRemote('playerEnterColshape');
                //})            
                const rpc = require('./accounts/rage-rpc.min.lib');

                let inShape = false;
                
                mp.keys.bind(0x45, true, function() {
                           if (mp.players.local.vehicle)return;
                  if (inShape) {
                    mp.events.callRemote('colshapeItem:CLIENT');
                  }
                });
                
                mp.events.add('client::changePickUpItemInfo', (toggle) => {
                  inShape = toggle;
                })
                
                rpc.register('client::getHashByName', (name) => {
                  return mp.game.gameplay.getHashKey(name);
                })
//mp.events.add('vip', () => {
    //mp.events.callRemote('vip', [123, 456 ])
    //browserrr.destroy();
    //mp.gui.cursor.show(false, false);
 //});
//let = vip;
//mp.events.add('vipp', () => {
    //vip.destroy();
  //  mp.browsers.new('package://accounts/cef/vip/index.html')
    //mp.gui.cursor.show(true, true);
//});






//FRAK

mp.events.add('uponatik', () => {
    mp.events.call("CEF:NOTIFI:ADD", "warning", 5500, "Пополнить баланс можно создав тикет в нашем дискорде", "Информация"); // вызов с клиентской стороны
    mp.events.call("CEF:NOTIFI:ADD", "warning", 5500, "Colt DM -> #Donate", "Информация"); // вызов с клиентской стороны
});
mp.events.add('plusdonatik', () => {
    mp.events.call("CEF:NOTIFI:ADD", "warning", 5500, "Пополнить баланс можно создав тикет в нашем дискорде", "Информация"); // вызов с клиентской стороны
    mp.events.call("CEF:NOTIFI:ADD", "warning", 5500, "Colt DM -> #Donate", "Информация"); // вызов с клиентской стороны
});



// mp.keys.bind(0x45, true, () => {
// if (mp.players.local.position.dist(hevik.position) < 2) {
//     hevik.destroy();
//     mp.players.local.playAnimation('pickup_object', 'pickup_low', 1, 549);
//     setTimeout(() => {
//         mp.players.local.stopAnimation();
//     }, 1800);
// } else {
//     mp.game.graphics.notify('Ты далеко');
// }
// });













mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('AirDropLute');
});
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('gunshop');
});
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('twoghetto');
});
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('blacksell');
});
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('GoUgonka');
});
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('autosalonn');
});

let famBrowser
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    // mp.events.callRemote('createfam');
    // famBrowser = mp.browsers.new('package://accounts/cef/famCreate/index.html');
});
let black;
var blackk = false;
mp.events.add('blackselll', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('s:udpatecraft');
    if (blackk) {
        if (black.active) {
            black.active = false;
            mp.gui.cursor.show(false, false);
        } else {
            black.active = true;
            mp.gui.cursor.show(true, true);
        }
    } else {
        blackk = true;
        mp.gui.cursor.show(true, true);
        black = mp.browsers.new('package://accounts/cef/craft/index.html');
    }
         mp.keys.bind(0x1B, false, () => {
            if (blackk) {
                black.active = false;
                mp.gui.cursor.show(false, false);
                blackk = false;
            }
        });
});
mp.events.add('client::updateCraftInfo', (playerResources) => {
    if (mp.browsers.exists(black)) {
        black.execute(`window.playerInfoitemCraft(${playerResources})`);
    }
})
let gunshop;
var gunshopp = false;
mp.events.add('gs', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    if (gunshopp) {
        if (gunshop.active) {
            gunshop.active = false;
            mp.gui.cursor.show(false, false);
        } else {
            gunshop.active = true;
            mp.gui.cursor.show(true, true);
        }
    } else {
        gunshopp = true;
        mp.gui.cursor.show(true, true);
        gunshop = mp.browsers.new('package://accounts/cef/gunshop/index.html');
    }
         mp.keys.bind(0x1B, false, () => {
            if (gunshopp) {
                gunshop.active = false;
                mp.gui.cursor.show(false, false);
                gunshopp = false;
            }
        });
});
mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('barberxdxd');
});
mp.events.add('barberxd', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('joinBarbers');
});
let autosalon;
var autosalonn = false;
mp.events.add('autosalon', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    if (autosalonn) {
        if (autosalon.active) {
            autosalon.active = false;
            mp.gui.cursor.show(false, false);
        } else {
            autosalon.active = true;
            mp.gui.cursor.show(true, true);
        }
    } else {
        autosalonn = true;
        mp.gui.cursor.show(true, true);
        autosalon = mp.browsers.new('package://accounts/cef/autosalon/index.html');
    }
         mp.keys.bind(0x1B, false, () => {
            if (autosalonn) {
                autosalon.active = false;
                mp.gui.cursor.show(false, false);
                autosalonn = false;
            }
        });
});
mp.events.add('koburka', () => {//кобура
    mp.events.callRemote('koburka')
    //odezda.destroy();
});

 mp.events.add('maskone', () => {//маска N1
    mp.events.callRemote('maskone')
    //odezda.destroy();
});

 mp.events.add('masktwo', () => {//маска N2
    mp.events.callRemote('masktwo')
    //odezda.destroy();
});
mp.events.add('maskthree', () => {//маска N3
    mp.events.callRemote('maskthree')
    //odezda.destroy();
 });
mp.events.add('bronik', () => {//броненагрузка
    mp.events.callRemote('bronik')
    //odezda.destroy();
 });

 mp.events.add('noclose', () => {
    inv.destroy();
    mp.gui.cursor.show(false, false);
    invv = false;
    //odezda.destroy();

 });
 mp.keys.bind(0x73, true, () => {
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('checkapanell');
});
let apanel;
var apanell = false;
mp.events.add('checkapanel', () => {
     if (mp.players.local.isTypingInTextChat)
         return;
     if (apanell) {
        if (apanel.active) {
            apanel.active = false;
            mp.gui.cursor.show(false, false);
            mp.events.callRemote('yesno');
        } else {
            apanel.active = true;
            mp.gui.cursor.show(true, true);
            mp.events.callRemote('noyes');
        }
     } else {
        apanell = true;
        mp.gui.cursor.show(true, true);
        apanel = mp.browsers.new('package://accounts/cef/newApanel/index.html');
        mp.events.callRemote('noyes');

     }
     mp.keys.bind(0x1B, false, () => {
        if (apanell) {
            apanel.active = false;
            mp.gui.cursor.show(false, false);
            mp.events.callRemote('yesno');
            apanell = false;
        }
    });
});
mp.events.add('adm1', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('admm1');
})
mp.events.add('adm2', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('admm2');
})
mp.events.add('adm3', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    mp.events.callRemote('admm3');
})


// mp.keys.bind(0x4C, true, () => {
//     mp.browsers.new('package://accounts/cef/newmenu/index.html');
// });






// mp.events.add('playerDeath', () => {
//       mp.game.streaming.requestAnimDict("amb@world_human_bum_slumped@male@laying_on_left_side@base");
//       mp.players.local.taskPlayAnim("amb@world_human_bum_slumped@male@laying_on_left_side@base","base", 0, 0, 100, 1, 0.0, false, false, false);
//       mp.players.local.stopAnimTask("amb@world_human_bum_slumped@male@laying_on_left_side@base","base", 0.1);
// })






mp.keys.bind(0x45, true, () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('odezdaxdd');
});
mp.events.add('odezdaxd', () => {
    if (mp.players.local.getVariable('isLogin'))
    return;
    if (mp.players.local.isTypingInTextChat)
        return;
    mp.events.callRemote('jc');
});


// let respawn;
// var respawnn = false;
let voice;
mp.events.add('playerReady', () => {
    voice = mp.browsers.new('package://accounts/cef/mirko/index.html');
    voice.active = false;
});

const Use3d = true;
const UseAutoVolume = false;

const MaxRange = 15.0;

let voiceChatEnabled = false;
mp.keys.bind(0x4E, true, function() {
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.getVariable('DrygoiSoxik')) return;
    if (mp.players.local.isTypingInTextChat) return;
    mp.voiceChat.muted = false; // Включаем голосовой чат при нажатии клавиши E
    voice.active = true;
    mp.events.callRemote('c:onVoiceIkon');
    
});

mp.keys.bind(0x4E, false, function() {
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.getVariable('DrygoiSoxik')) return;
    if (mp.players.local.isTypingInTextChat) return;

    mp.voiceChat.muted = true; // Отключаем голосовой чат при отпускании клавиши E
    mp.events.callRemote('c:offVoiceIkon');
    if (!mp.browsers.exists(voice)) return;
    if (voice.active) {
        voice.active = false;
    } else {
        voice.active = true;
    }
});


mp.keys.bind(0x76, true, function() {
    if (mp.players.local.getVariable('isLogin')) return;
    if (mp.players.local.getVariable('DrygoiSoxik')) return;
    if (mp.players.local.isTypingInTextChat) return;
    mp.events.call("CEF:NOTIFI:ADD", "warning", 3000, "Войс чат перезагружен", "Информация"); // вызов с клиентской стороны
    mp.voiceChat.muted = true;
    mp.events.callRemote('c:offVoiceIkon');
    if(voice.active){
        voice.active = false;
    }
    });
// mp.keys.bind(0x4E, true, function() {
//     mp.voiceChat.muted = !mp.voiceChat.muted;
//     let status = (!mp.voiceChat.muted) ? "~g~enabled" : "~r~disabled";
//     mp.game.graphics.notify("Voice Chat: " + status);
// });

let g_voiceMgr =
{
	listeners: [],
	
	add: function(player)
	{
		this.listeners.push(player);
		
		player.isListening = true;		
		mp.events.callRemote("add_voice_listener", player);
		
		if(UseAutoVolume)
		{
			player.voiceAutoVolume = true;
		}
		else
		{
			player.voiceVolume = 1.0;
		}
		
		if(Use3d)
		{
			player.voice3d = true;
		}
	},
	
	remove: function(player, notify)
	{
		let idx = this.listeners.indexOf(player);
			
		if(idx !== -1)
			this.listeners.splice(idx, 1);
			
		player.isListening = false;		
		
		if(notify)
		{
			mp.events.callRemote("remove_voice_listener", player);
		}
	}
};

mp.events.add("playerQuit", (player) =>
{
	if(player.isListening)
	{
		g_voiceMgr.remove(player, false);
	}
});

setInterval(() =>
{
	let localPlayer = mp.players.local;
	let localPos = localPlayer.position;
	
	mp.players.forEachInStreamRange(player =>
	{
		if(player != localPlayer)
		{
			if(!player.isListening)
			{
				const playerPos = player.position;		
				let dist = mp.game.system.vdist(playerPos.x, playerPos.y, playerPos.z, localPos.x, localPos.y, localPos.z);
				
				if(dist <= MaxRange)
				{
					g_voiceMgr.add(player);
				}
			}
		}
	});
	
	g_voiceMgr.listeners.forEach((player) =>
	{
		if(player.handle !== 0)
		{
			const playerPos = player.position;		
			let dist = mp.game.system.vdist(playerPos.x, playerPos.y, playerPos.z, localPos.x, localPos.y, localPos.z);
			
			if(dist > MaxRange)
			{
				g_voiceMgr.remove(player, true);
			}
			else if(!UseAutoVolume)
			{
				player.voiceVolume = 1 - (dist / MaxRange);
			}
		}
		else
		{
			g_voiceMgr.remove(player, true);
		}
	});
}, 500);









class eventsSnipers {
    browserSnipers;
    constructor() {
        mp.events.add('c:openevents', () => {
            if (!this.browserSnipers){
            this.browserSnipers = mp.browsers.new('package://accounts/cef/eventSnipers/index.html');
            }
        });
        mp.events.add('joinmp', () => {
            mp.events.callRemote('teleportation');
        });
        mp.events.add('c:deleveevents', () => {
            if(this.browserSnipers){
                this.browserSnipers.destroy();
                this.browserSnipers = null;
                mp.gui.cursor.show(false,false);
            }
        });
        mp.events.add('leavemp', () => {
            mp.events.callRemote('nomp');
        });
        mp.events.add('cancelmp', () => {
            if(this.browserSnipers){
                this.browserSnipers.destroy();
                this.browserSnipers = null;
                mp.gui.cursor.show(false,false);
            }
        });
    };
};
const EventsSnipers = new eventsSnipers();

let clothescef;
mp.events.add('c:cameraOn', () => {
    const sceneryCamera = mp.cameras.new('default', new mp.Vector3(429.872, -802.055, 29.6911, -1.2314), new mp.Vector3(), 50);
    // mp.game.cam.setGameplayCamRelativeHeading(90);
    sceneryCamera.setActive(true);
    mp.game.cam.setGameplayCamRelativeHeading(0);
    mp.game.cam.renderScriptCams(true, false, 0, true, false);
    if (!clothescef){
        clothescef = mp.browsers.new('package://accounts/cef/clothes/index.html');
        browser.active = false;
        mp.gui.chat.show(false);
    };
    mp.keys.bind(0x1B, false, () => {
        if (clothescef) {
            mp.events.callRemote('leaveClothess');
            clothescef.destroy();
            mp.gui.cursor.show(false, false);
            clothescef = null;
            browser.active = true;
        }
        mp.game.cam.renderScriptCams(false, false, 0, true, false);
        setTimeout(() => {
            mp.gui.cursor.show(false,false);
            browser.active = true;
        }, 500);
    });
        // mp.gui.cursor.show(true,true);
});
mp.events.add('c:cameraOff', () => {
    mp.game.cam.renderScriptCams(false, false, 0, true, false);
    if(clothescef){
        clothescef.destroy();
        clothescef = null;
        mp.gui.cursor.show(false,false);
    }
    setTimeout(() => {
        browser.active = true;
        mp.gui.chat.show(true);
        mp.gui.cursor.show(false,false);
    }, 500);
});

mp.events.add('render', () => {
    if (!mp.players.local.getVariable('noUseControls')) return;
    mp.gui.cursor.show(true,true);
    mp.game.controls.disableControlAction(0, 21, true); /// бег
    mp.game.controls.disableControlAction(0, 22, true); /// прыжок
    mp.game.controls.disableControlAction(0, 31, true); /// вперед назад
    mp.game.controls.disableControlAction(0, 30, true); /// влево вправо
    mp.game.controls.disableControlAction(0, 24, true); /// удары
    mp.game.controls.disableControlAction(0, 25, true); /// INPUT_AIM
    mp.game.controls.disableControlAction(0, 257, true); /// стрельба
    mp.game.controls.disableControlAction(1, 200, true); // esc
    mp.game.controls.disableControlAction(0, 140, true); /// удары R
    mp.game.controls.disableControlAction(24, 37, true); /// Tab
    mp.game.controls.disableControlAction(0, 257, true); // INPUT_ATTACK2
});
mp.events.add('c:leaveLeave', () => {
    mp.game.cam.renderScriptCams(false, false, 0, true, false);
    if(clothescef){
        clothescef.destroy();
        clothescef = null;
        mp.gui.cursor.show(false,false);
        mp.events.callRemote('leaveClothess');
        browser.active = true;
        mp.gui.chat.show(true);
        setTimeout(() => {
            mp.gui.cursor.show(false,false);
        }, 500);
    }
});

mp.events.add('savesss', () => {
    mp.events.callRemote('saveclothes');
});

mp.events.add('spawncar', (argsCars) => {
    mp.events.callRemote('spawncar', argsCars);
    mp.gui.cursor.show(false,false);
});


mp.events.add('autoSalonBuy', (argsAutosalonBuyCar) => {
    mp.events.callRemote('s:autoSalonBuy', argsAutosalonBuyCar);
});

mp.events.add('autoDonateBuy', (argsDonatenBuyCar) => {
    mp.events.callRemote('s:autoDonateBuy', argsDonatenBuyCar);
});
mp.events.add('sendMessageToPlayer', () => {
    mp.gui.chat.push('Привет');
});
// mp.events.add('checkplayer', (static) => {
//     mp.gui.chat.push(`Отправлен playerId на сервер: ${static}`);
//     mp.gui.chat.push('А вот фигушки тебе! :) В разработке блин!');
//     mp.events.callRemote('groupInvite', static)
// });


mp.events.add('c:newLoc', (locationID) => {
    mp.events.callRemote('s:newLoc', locationID)
});


mp.events.add('buycars', (argsCars) => {
    mp.events.callRemote('buycarss', argsCars)
});















let barberCef;
mp.events.add('c:cameraOnBarber', () => {
    const sceneryCamera = mp.cameras.new('default', new mp.Vector3(133.725, -1710.732, 30.3, 52.540), new mp.Vector3(), 50);
    // mp.game.cam.setGameplayCamRelativeHeading(90);
    sceneryCamera.setActive(true);
    mp.game.cam.setGameplayCamRelativeHeading(0);
    mp.game.cam.renderScriptCams(true, false, 0, true, false);
    if (!barberCef){
        barberCef = mp.browsers.new('package://accounts/cef/barber/index.html');
        browser.active = false;
        mp.gui.chat.show(false);
    };
    mp.keys.bind(0x1B, false, () => {
        if (barberCef) {
            mp.events.callRemote('leaveBarberr');
            barberCef.destroy();
            mp.gui.cursor.show(false, false);
            barberCef = null;
            browser.active = true;
        }
        mp.game.cam.renderScriptCams(false, false, 0, true, false);
        setTimeout(() => {
            mp.gui.cursor.show(false,false);
            browser.active = true;
        }, 500);
    });
        // mp.gui.cursor.show(true,true);
});
mp.events.add('c:cameraOffBarber', () => {
    mp.game.cam.renderScriptCams(false, false, 0, true, false);
    if(barberCef){
        barberCef.destroy();
        barberCef = null;
        mp.gui.cursor.show(false,false);
    }
    setTimeout(() => {
        browser.active = true;
        mp.gui.chat.show(true);
        mp.gui.cursor.show(false,false);
    }, 500);
});

mp.events.add('c:leaveBarber', () => {
    mp.game.cam.renderScriptCams(false, false, 0, true, false);
    if(barberCef){
        barberCef.destroy();
        barberCef = null;
        mp.gui.cursor.show(false,false);
        mp.events.callRemote('leaveBarberr');
        browser.active = true;
        mp.gui.chat.show(true);
        setTimeout(() => {
            mp.gui.cursor.show(false,false);
        }, 500);
    }
});

mp.events.add('savesBarber', () => {
    mp.events.callRemote('saveBarber');
});
































// // [Controls]
// let DroneForward = 172;
// let DroneBackwards = 173;
// let DroneLeft = 174;
// let DroneRight = 175;
// let DroneRotateLeft = 85;
// let DroneRotateRight = 86;
// let DroneGoUp = 22;
// let DroneGoDown = 62;

// let ToggleDroneCameraKey = 0x4E;

// let TazeKey = 0x54;
// let TazeModifierKey = null;

// let ToggleNightVisionKey = 0x75;//0x69;
// let ToggleThermalVisionKey = 0x76;//0x63;

// // [DroneModel]
// let DroneModel = "ch_prop_casino_drone_02a";

// // [DroneConfiguration]
// let MaxVelocity = 250;
// let Agility = 1;
// let FlyingSound = true;
// let CanDisableEngines = true;
// let CanTaze = true;
// let HasNightVision = false;
// let HashermalVision = false;

// // [DroneCamera]
// let OffsetX = 0;
// let OffsetY = -0.15;
// let OffsetZ = -0.12;

// // [Objects]
// let droneObject = null;
// let camObject = null;

// // [Basic]
// let isDroneActive = false;
// let toggleDroneKey = 0x78;
// let droneSpeed = 0.125;

// let direction;
// let coords;
// let rotation;

// // [Features]
// mp.keys.bind(ToggleNightVisionKey, false, function() {
//     if (!isDroneActive) return;
//     if (HashermalVision) return;
//     HasNightVision = !HasNightVision;
//     mp.game.graphics.setNightvision(HasNightVision);
// });

// mp.keys.bind(ToggleThermalVisionKey, false, function() {
//     if (!isDroneActive) return;
//     if (HasNightVision) return;
//     HashermalVision = !HashermalVision;
//     mp.game.graphics.setSeethrough(HashermalVision);
// });

// mp.keys.bind(ToggleDroneCameraKey, false, function() {
//     if (camObject)
//     {
//         mp.players.local.freezePosition(false);
//         mp.game.graphics.setNightvision(false);
//         mp.game.graphics.setSeethrough(false);
//         camObject.detach();
//         camObject.destroy();
//         camObject = null;
//         mp.game.cam.renderScriptCams(false, true, 500, false, false);
//     } 
//     else
//     {
//         mp.players.local.freezePosition(true);
//         mp.game.graphics.setSeethrough(HashermalVision);
//         mp.game.graphics.setNightvision(HasNightVision);
//         camObject = mp.cameras.new('DroneCamera', new mp.Vector3(droneObject.position.x, droneObject.position.y, droneObject.position.z), new mp.Vector3(0,0,0), 40);
//         camObject.attachTo(droneObject.handle, OffsetX, OffsetY, OffsetZ, false);
//         camObject.setActive(true);
//         mp.game.cam.renderScriptCams(true, true, 500, true, false);
//     }
// });

// // [Start]
// mp.keys.bind(toggleDroneKey, false, function() {
//     if (mp.players.local.vehicle) return;
//     isDroneActive = !isDroneActive;
//     mp.players.local.freezePosition(isDroneActive);
//     mp.game.ui.displayRadar(!isDroneActive);
//     mp.game.ui.displayHud(!isDroneActive);
//     if (isDroneActive)
//     {
//         mp.game.graphics.setSeethrough(HashermalVision);
//         mp.game.graphics.setNightvision(HasNightVision);
//         mp.game.audio.playSound(-1, "Hack_Success", "DLC_HEIST_BIOLAB_PREP_HACKING_SOUNDS", true, 0, true);
//         droneObject = mp.objects.new(mp.game.joaat(DroneModel), new mp.Vector3(mp.players.local.position.x, mp.players.local.position.y, mp.players.local.position.z + 5));
//         camObject = mp.cameras.new('DroneCamera', new mp.Vector3(mp.players.local.position.x, mp.players.local.position.y, mp.players.local.position.z + 5), new mp.Vector3(0,0,0), 40);
//         camObject.attachTo(droneObject.handle, OffsetX, OffsetY, OffsetZ, false);
//         camObject.setActive(true);
//         mp.game.cam.renderScriptCams(true, true, 500, true, false);
//     } 
//     else if (!isDroneActive)
//     {
//         mp.game.audio.playSound(-1, "QUIT", "HUD_FRONTEND_DEFAULT_SOUNDSET", true, 0, true);
//         if (droneObject)
//         {
//             droneObject.destroy();
//             droneObject = null;
//         }
//         if (camObject)
//         {
//             camObject.detach();
//             camObject.destroy();
//             camObject = null;
//             mp.game.cam.renderScriptCams(false, true, 500, false, false);
//         }
//         mp.game.graphics.setSeethrough(false);
//         mp.game.graphics.setNightvision(false);
//     }
// });

// mp.events.add("render", () =>
// {
//     if (isDroneActive && droneObject != null && camObject != null && !mp.players.local.vehicle)
//     {
//         direction = camObject.getDirection();
//         coords = camObject.getCoord();
//         rotation = camObject.getRot(2);

//         let updated = false;
//         const curposition = droneObject.position;
//         if (mp.game.controls.isControlPressed(3, DroneForward)) {
//             if (droneObject.getHeightAboveGround() <= 1) return;
//             if (pointingAt(10)) return camObject.shake("MEDIUM_EXPLOSION_SHAKE", 12);
//             if (pointingAt(20))
//             {
//                 camObject.stopShaking(true);
//                 mp.game.audio.playSound(-1, "Beep_Red", "DLC_HEIST_HACKING_SNAKE_SOUNDS", true, 0, true);
//             }
//             curposition.x += direction.x * droneSpeed;
//             curposition.y += direction.y * droneSpeed;
//             curposition.z += direction.z * droneSpeed;
//             updated = true;
//         }
//         if (mp.game.controls.isControlPressed(3, DroneBackwards)) {
//             if (droneObject.getHeightAboveGround() <= 1) return;
//             if (pointingAt(10)) return camObject.shake("MEDIUM_EXPLOSION_SHAKE", 12);
//             if (pointingAt(20))
//             {
//                 camObject.stopShaking(true);
//                 mp.game.audio.playSound(-1, "Beep_Red", "DLC_HEIST_HACKING_SNAKE_SOUNDS", true, 0, true);
//             }
//             curposition.x -= direction.x * droneSpeed;
//             curposition.y -= direction.y * droneSpeed;
//             curposition.z -= direction.z * droneSpeed;
//             updated = true;
//         }
//         if (mp.game.controls.isControlPressed(3, DroneLeft)) {
//             if (droneObject.getHeightAboveGround() <= 1) return;
//             if (pointingAt(10)) return camObject.shake("MEDIUM_EXPLOSION_SHAKE", 12);
//             if (pointingAt(20))
//             {
//                 camObject.stopShaking(true);
//                 mp.game.audio.playSound(-1, "Beep_Red", "DLC_HEIST_HACKING_SNAKE_SOUNDS", true, 0, true);
//             }
//             curposition.x += (-direction.y) * droneSpeed;
//             curposition.y += direction.x * droneSpeed;
//             updated = true;
//         }
//         if (mp.game.controls.isControlPressed(3, DroneRight)) {
//             if (droneObject.getHeightAboveGround() <= 1) return;
//             if (pointingAt(10)) return camObject.shake("MEDIUM_EXPLOSION_SHAKE", 12);
//             if (pointingAt(20))
//             {
//                 camObject.stopShaking(true);
//                 mp.game.audio.playSound(-1, "Beep_Red", "DLC_HEIST_HACKING_SNAKE_SOUNDS", true, 0, true);
//             }
//             curposition.x -= (-direction.y) * droneSpeed;
//             curposition.y -= direction.x * droneSpeed;
//             updated = true;
//         }
//         if (mp.game.controls.isControlPressed(3, DroneGoUp)) {
//             curposition.z += droneSpeed;
//             updated = true;
//         }
//         if (mp.game.controls.isControlPressed(3, DroneGoDown)) {
//             if (droneObject.getHeightAboveGround() <= 1) return;
//             curposition.z -= droneSpeed;
//             updated = true;
//         }
//         if (mp.game.controls.isControlPressed(2, DroneRotateLeft)) {
//             camObject.setRot(rotation.x, rotation.y, rotation.z + 0.4, 1);
//             droneObject.setRotation(rotation.x, rotation.y, rotation.z, 1, true);
//         }
//         if (mp.game.controls.isControlPressed(2, DroneRotateRight)) {
//             camObject.setRot(rotation.x, rotation.y, rotation.z - 0.4, 1);
//             droneObject.setRotation(rotation.x, rotation.y, rotation.z, 1, true);
//         }
//         if (updated)
//         {
//             droneObject.position = new mp.Vector3(curposition.x, curposition.y, curposition.z);
//             camObject.setCoord(curposition.x, curposition.y, curposition.z);
//         }
//         if (mp.keys.isDown(2) === true) {
//             if (CanTaze)
//             {
//                 shootTaze();
//             }
//         }
//     }
// });

// function shootTaze()
// {
//     mp.game.gameplay.shootSingleBulletBetweenCoords(droneObject.position.x, droneObject.position.y, droneObject.position.z, mp.players.local.position.x, mp.players.local.position.y, mp.players.local.position.z, 1, false, mp.game.joaat("weapon_stungun"), droneObject.handle, false, false, 1.0);
//     CanTaze = false;
//     setTimeout(() => {
//         CanTaze = true;
//     }, 3000);
// }

// function pointingAt(distance) {
//     const camera = mp.cameras.new("gameplay"); // gets the current gameplay camera

//     let position = camera.getCoord(); // grab the position of the gameplay camera as Vector3

//     let direction = camera.getDirection(); // get the forwarding vector of the direction you aim with the gameplay camera as Vector3

//     let farAway = new mp.Vector3((direction.x * distance) + (position.x), (direction.y * distance) + (position.y), (direction.z * distance) + (position.z)); // calculate a random point, drawn on a invisible line between camera position and direction (* distance)

//     let result = mp.raycasting.testPointToPoint(position, farAway, null, [1, 2, 4, 8, 16, 32, 64, 128, 256]); // now test point to point - intersects with map and objects [1 + 16]

//     return result; // and return the result ( undefined, if no hit )
// }




mp.events.add('SetSpawnEvents', () => {
    mp.events.callRemote('s:SetSpawnEvents')
});


mp.events.add('StartSetnew', (textPlayer) => {
    mp.gui.chat.push(`Отправлен текст на сервер (КЛИЕНТ): ${textPlayer}`);
    mp.events.callRemote('s:StartEvents', textPlayer)
});
mp.events.add('CloseEvents', () => {
    mp.events.callRemote('s:CloseEvents')
});




mp.events.add('craftItem', (argsCraftItems) => {
    mp.events.callRemote('s:craftItem', argsCraftItems)
})



mp.events.add('newCapture', () => {
    mp.events.callRemote('s:newCapture')
})
mp.events.add('closeCapture', () => {
    mp.events.callRemote('s:closeCapture')
})
mp.events.add('joinCapture', () => {
    mp.events.callRemote('s:joinCapture')
})

// mp.events.add('c:createBlipCapture', () => {
//     const blip = mp.game.invoke('0x46818D79B1F7499A', -1790, -222, 0, 9999); // addBlipForRadius
//     mp.game.invoke('0xDF735600A4696DAF', blip, 10); // setBlipSprite
//     mp.game.invoke('0x45FF974EEE1C8734', blip, 0); // setBlipAlpha
//     mp.game.invoke('0x13127EC3665E8EE1', blip, true); // showOutlineIndicatorOnBlip
//     mp.game.invoke('0x03D7FB09E75D6B7E', blip, 84); // setBlipColour
//     mp.game.invoke('0x14892474891E09EB', blip, 0, 255, 0); // setBlipSecondaryColour
// });

//                                             mp.keys.bind(0x45, true, () => {
//                                                 if (mp.players.local.getVariable('isLogin')) return;
//                                                 if (mp.players.local.getVariable('CaptureIn')) return;
//                                                 if (mp.players.local.isTypingInTextChat) return;
//                                                 mp.events.callRemote('s:checkOpenCef');
//                                             });

//                                             mp.keys.bind(0x4D, true, () => {
//                                                 if (mp.players.local.getVariable('isLogin')) return;
//                                                 if (mp.players.local.getVariable('CaptureIn')) return;
//                                                 if (mp.players.local.isTypingInTextChat) return;
//                                                 mp.events.callRemote('s:checkOpenCefFamMenu');
//                                             });



//                                             let cefCreateFam;
//                                             let cefCreateFamm = false;
//                                             mp.events.add('playerReady', () => {
//                                                 cefCreateFam = mp.browsers.new('package://accounts/cef/createfam/index.html');
//                                                 cefCreateFam.active = false;
//                                                 cefCreateFamm = false;
//                                             });




//                                             mp.events.add('c:openCefCreateFamily', () => {
//                                                 if (mp.players.local.getVariable('isLogin')) return;
//                                                 if (mp.players.local.getVariable('CaptureIn')) return;
//                                                 if (mp.players.local.isTypingInTextChat) return;
//                                                 if (cefCreateFamm){
//                                                     cefCreateFam.active = false;
//                                                     cefCreateFamm = false;
//                                                     mp.gui.cursor.show(false,false);
//                                                 } else {
//                                                     cefCreateFam.active = true;
//                                                     cefCreateFamm = true;
//                                                     mp.gui.cursor.show(true,true);
//                                                 }
//                                                 mp.keys.bind(0x1B, false, () => {
//                                                     if (cefCreateFamm) {
//                                                         cefCreateFam.active = false;
//                                                         mp.gui.cursor.show(false, false);
//                                                         cefCreateFamm = false;
//                                                     }
//                                                 });
//                                             })

//                                             mp.events.add('c:createFamily', (nameFamily) => {
//                                                 mp.events.callRemote('s:createFamily', nameFamily)
//                                             });


//                                             let cefMenuFamily;
//                                             let cefMenuFamilyy = false;
//                                             mp.events.add('playerReady', () => {
//                                                 cefMenuFamily = mp.browsers.new('package://accounts/cef/fammenu/index.html');
//                                                 cefMenuFamily.active = false;
//                                                 cefMenuFamilyy = false;
//                                             });

//                                             mp.events.add('c:checkOpenCefFamMenu', () => {
//                                                 if (mp.players.local.getVariable('isLogin')) return;
//                                                 if (mp.players.local.getVariable('CaptureIn')) return;
//                                                 if (mp.players.local.isTypingInTextChat) return;
//                                                 if (cefMenuFamilyy){
//                                                     cefMenuFamily.active = false;
//                                                     cefMenuFamilyy = false;
//                                                     mp.gui.cursor.show(false,false);
//                                                 } else {
//                                                     cefMenuFamily.active = true;
//                                                     cefMenuFamilyy = true;
//                                                     mp.gui.cursor.show(true,true);
//                                                 }
//                                                 mp.keys.bind(0x1B, false, () => {
//                                                     if (cefMenuFamilyy) {
//                                                         cefMenuFamily.active = false;
//                                                         mp.gui.cursor.show(false, false);
//                                                         cefMenuFamilyy = false;
//                                                     }
//                                                 });
//                                             })


//                                             mp.events.add('client::FamilyMenu', (familyName, membersAll, membersOnline, reputationFamily) => {
//                                                 if (!mp.browsers.exists(cefMenuFamily)) return;
//                                                 cefMenuFamily.execute(`window.playerBanned("${familyName}", "${membersAll}", "${membersOnline}", "${reputationFamily}")`)
//                                             });


//                                             mp.events.add('c:captureFamily', (captureFamily) => {
//                                                 mp.events.callRemote('s:captureFamily', captureFamily)
// })

